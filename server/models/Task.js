// Task status values - single source of truth for all Task models
export const TASK_STATUSES = ["Todo", "In_Progress", "Review", "Done"];

// Legacy mongoose schema has been removed from this file as the server 
// package no longer depends on mongoose and this schema was unused.
// Use Prisma for all new database operations.
